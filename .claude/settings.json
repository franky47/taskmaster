{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; if echo \"$f\" | grep -qE '\\.ts$'; then ./node_modules/.bin/oxfmt \"$f\"; fi; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
